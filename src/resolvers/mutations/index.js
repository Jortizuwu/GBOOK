const roleMutations = require('./role')
const userMutations = require('./user')
const authMutations = require('./auth')
const statusMutations = require('./status')
const contentMutations = require('./content')
const bookMutations = require('./book')

module.exports = {
  roleMutations,
  userMutations,
  authMutations,
  statusMutations,
  contentMutations,
  bookMutations
}
